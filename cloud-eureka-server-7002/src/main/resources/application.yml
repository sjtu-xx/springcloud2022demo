server:
  port: 7002
eureka:
  instance:
    hostname: eureka7002
  client:
    # 不注册自己
    register-with-eureka: false
    # 不检索服务
    fetch-registry: false
    service-url:
      defaultZone: http://eureka7001:7001/eureka/