server:
  port: 8401
spring:
  application:
    name: sentinel8401
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    sentinel:
      transport:
        dashboard: localhost:8080
#        port: 8719
management:
  endpoints:
    web:
      exposure:
        include: '*'